# Valkey Maze

## Setup

1. Activate the virtual environment:
   ```
   source venv/bin/activate
   ```

2. Install dependencies (if needed):
   ```
   pip install svgwrite
   ```

## Usage

Run the main script to generate grids:
```
python main.py
```

This will create SVG files with triangular grids.